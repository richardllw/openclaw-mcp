{
  "name": "openclaw-mcp",
  "version": "0.2.0",
  "private": true,
  "description": "Local stdio MCP bridge exposing OpenClaw gateway tools to Claude Code and other MCP clients",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "regen": "node regen-tools-schema.mjs",
    "test": "node --test server.integration.test.js pending-yield-store.test.js"
  }
}
